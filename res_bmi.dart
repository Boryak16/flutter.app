import 'package:flutter/material.dart';

class REsBmi extends StatelessWidget {
  const REsBmi({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
